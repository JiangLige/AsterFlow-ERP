server:
  port: 3001

spring:
  application:
    name: demo-erp-server
  datasource:
    url: jdbc:mysql://localhost:3306/demo_erp?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 3s

jwt:
  secret: demo-erp-secret-key-change-me
  expire-hours: 24